language: julia
os: linux
julia:
  - 1.0
  - 1.3
  - nightly
notifications:
  email: false
after_success:
  - julia --project=coverage coverage/submit.jl
  - julia --project=docs docs/deploy.jl
jobs:
  allow_failures:
    - julia: nightly
